/** @deprecated Prefer CSS vars — var(--dur-instant/fast/base/slow/reveal) in styles. Keep for setTimeout/animationDelay JS values. */
export const DUR = {
  instant: 80,
  fast: 150,
  base: 250,
  slow: 400,
  reveal: 600,
};
